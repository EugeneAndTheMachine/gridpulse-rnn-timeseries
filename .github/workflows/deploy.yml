name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment placeholder
        run: echo "Deploy is intentionally skipped until infrastructure credentials are configured."
